<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign in to La Réserve</title>

    <link rel="icon" href="../logo.ico" type="image/x-icon" />
    <link rel="stylesheet" href="css/bootstrap.min.css" />
    <link rel="stylesheet" href="css/sign-in.css" />
  </head>

  <body class="text-center">
    <main class="form-signin w-100 m-auto">
      <form action="/login" method="POST">
        <a href="/">
          <img class="mb-4" src="../logo.ico" alt="" width="72" height="57" />
        </a>
        <h1 class="h3 mb-3 fw-normal">Sign in</h1>
        <% if (messages.error) { %>
        <div class="error" style="color: red"><%- messages.error %></div>
        <% } %>
        <div class="form-floating">
          <input
            type="email"
            name="username"
            class="form-control"
            id="email"
            placeholder="name@example.com"
            required
          />
          <label for="email">Email address</label>
        </div>
        <div class="form-floating mb-3">
          <input
            type="password"
            name="password"
            class="form-control"
            id="password"
            placeholder="Password"
            required
          />
          <label for="password">Password</label>
        </div>
        <div class="checkbox mb-3" style="float: left">
          <label>
            <input
              id="checkBox"
              type="checkbox"
              value="showPassword"
              onclick="passCheck()"
            />
            Show password
          </label>
        </div>
        <button class="w-100 btn btn-lg btn-primary mb-1" type="submit">
          Sign in
        </button>
        <p>New here? <a href="/register">Sign up</a></p>
        <a href="/forgot-password">Forgot your password?</a>
        <p class="mt-5 mb-3 text-muted">©2023 La Réserve</p>
      </form>
    </main>
    <script src="js/pass-check.js"></script>
  </body>
</html>
